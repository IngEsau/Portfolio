import type { Config } from "tailwindcss";

const config: Config = {
  theme: {
    extend: {
      colors: {        
        background: '#0A0A0A',         
        surface: '#121212'
      }
    },
  },
  plugins: [],
};
export default config;